# 角色聊天

一对一角色对话：可复用的角色定义，私有的用户记忆。角色定义按层来谈，不按封闭字段表。

## Language

**用户**:
产品层要维护的对象：和角色说话的人。
_Avoid_: 玩家, 客户

**角色定义**:
可被多场会话引用的一份规格，由稳定的角色标识和可变的角色版本组成。默认共享文风、人设和公共世界；还可以追加事先没列出的命名片段。不是一张必须填齐的字段表。
_Avoid_: 角色实例, 分身（分身是角色定义的一种来源，不是另一种对象）, 角色 schema

**角色标识**:
角色定义的稳定名字（包内 slug）。记忆、会话记录、验收都挂在「用户 × 角色标识」上。有用户记忆之后不要改；要决裂就发新标识。
_Avoid_: 显示名, 中文名, 版本号

**角色版本**:
角色包里片段的一版内容。换版本是换前缀，默认不换角色标识，因此不新开一份记忆。
_Avoid_: 角色标识

**片段**:
角色定义里一块有名字的内容，例如人设、文风、世界书，或后来新加的任意一篇。框架认已声明的片段，不规定必须有哪些字段。
_Avoid_: 字段, 必填项

**角色包**:
一次新增角色时交出来的那一份东西：若干文本片段，外加可选的策略。
_Avoid_: 插件

**策略**:
可选的、按角色或应用覆盖的组装与记忆行为。默认由框架提供；角色包可以不带策略。
_Avoid_: skill

**记忆策略**:
一种策略：决定本回合如何写入记忆、如何编译当前看法、如何检索进上下文。未声明时自动挂上框架默认策略；应用或角色包可以用名字换一份已注册的策略。写法只允许四个口：抽取、编译、检索、导出。
_Avoid_: 记忆（仓库本身）, 向量库, 自动生成的策略, 任意 hook

**抽取**:
记忆策略的口：从刚结束的回合提出新增、修正、收回。
_Avoid_: 训练, 微调

**编译**:
记忆策略的口：把记忆事件收成当前工作副本。
_Avoid_: 检索

**检索**:
记忆策略的口：按本轮用户的话，从工作副本取出要塞进上下文的记忆块。工作副本不够时，必须能回源到尚未入账的会话记录。
_Avoid_: 前缀

**导出**:
记忆策略的口：给出某「用户 × 角色定义」当前看法的明文。验收对账必须走这里。
_Avoid_: 向量快照

**Skill**:
给模型按需阅读的能力说明书，是文本片段的一种，不是策略代码。
_Avoid_: 策略, 工具实现

**验收剧本**:
角色包里可选的片段：扮演用的问句和必现/禁现，以及记忆用的对话脚本与导出断言。不是封闭字段表。
_Avoid_: 排行榜, 纯模型打分

**角色**:
产品层要维护的对象：用户面对的那个对话者。由角色定义来扮演。产品不展示会话列表。
_Avoid_: 机器人, agent

**关系**:
产品层的一对：「用户 × 角色标识」。记忆挂在关系上。一次说话请求只带用户和角色，不带会话。
_Avoid_: 会话（会话是框架层）

**会话**:
框架层：一场带起止的对话记录，创建时钉住当时的角色版本。新会话用当时的最新版本。产品可以不展示、不让用户选择会话。
_Avoid_: 房间, 群聊, 关系

**重开**:
框架提供的口：为一段关系新开一场会话（钉住此时最新角色版本，记忆仍挂在关系上）。何时调用由应用决定，不写进框架默认。
_Avoid_: 新开对话（产品概念）, 自动升版

**会话记录**:
一场会话持久化保存的消息全文。无论是否已抽取，原文都必须落库，不得因进窗口、压缩或抽取失败而丢弃。
_Avoid_: 记忆, 工作副本, 窗口

**记忆**:
属于一段关系的私有记录，不属于单场会话。默认形态是事件日志加工作副本；自定义记忆策略可以换形态，但不能改归属，也不能把不可导出的黑盒当成唯一真相。
_Avoid_: 上下文, 会话记录

**记忆事件**:
默认记忆策略里一条只追加的记录，类型为新增、修正或收回。
_Avoid_: 向量, 字段行

**工作副本**:
默认记忆策略根据记忆事件编译出的当前看法，供检索和塞进上下文。
_Avoid_: 真相（真相是事件日志，除非换了策略）

**上下文**:
某一轮推理时实际送给模型的消息窗口。默认菜谱是：稳定的角色定义前缀、近期会话、本轮检索到的记忆。它是瞬时组装，不是仓库。
_Avoid_: 记忆

**前缀**:
上下文里由角色定义片段组成、应尽量保持稳定的那一段。在 loop 里对应 `systemPrompt`。变化的记忆不得写入这里，否则从改动处起 KV cache 无法复用。
_Avoid_: 工作副本, 检索块

**回合**:
用户一条消息触发、直到这场会话的 loop 空闲的一次运行。回合结束要先抽取，再谈这轮该不该压缩会话。
_Avoid_: 请求, 线程

**待压缩段**:
一场会话里即将离开上下文窗口、改由摘要代表的那截消息。原文仍在会话记录里；是否已抽进记忆，是另一件事。
_Avoid_: 已删除的消息

**回源**:
检索发现工作副本可能不够时，回到尚未入账的会话记录去补抽或读原文。
_Avoid_: 导出

**同步抽取**:
框架提供的口：在本回合回答前把指定内容抽完。默认策略里，显式记住、收回、承诺走这里；其余抽取仍可异步。应用可以再调这个口。
_Avoid_: 默认抽取（默认是异步的）

**显式记住**:
用户要求写入记忆的话，例如「记住：我下周考试」。走同步抽取。
_Avoid_: 闲聊

**收回**:
用户否定或作废已有记忆的话。走同步抽取，以免下一句仍按旧事实答。
_Avoid_: 修正（修正是改内容，收回是作废）

**承诺**:
用户或角色做出的、以后要被问到的约定。走同步抽取。
_Avoid_: 心情, 闲聊

**运行时**:
一场会话上的活 loop。默认只在回合内存在；应用可以让它在回合之间保持，但仍按会话挂，不按角色常驻。对话循环用 Pi 的 agent-core，不套 coding-agent 的编码壳。
_Avoid_: 后台线程, 角色进程, pi CLI
