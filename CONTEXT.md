# 角色聊天框架

用于描述一对一角色聊天中的可复用角色内容、角色分身、会话、回合和记忆。

## Language

**角色定义（Character Definition）**:
可复用的角色内容模板，由任意命名片段组成，不保存用户、会话或记忆。
_Avoid_: 角色实例、分身、固定字段表

**分身（Persona）**:
用户实际对话的角色实例；它有稳定标识，引用一份角色定义，并可添加覆盖片段。
_Avoid_: 用户人设、角色定义、常驻 Agent

**用户（User）**:
由应用提供稳定标识的对话者。
_Avoid_: Persona、角色

**会话（Session）**:
一个用户与一个分身之间的一段有序消息记录。
_Avoid_: 记忆、运行时

**会话版本（Session Revision）**:
一次会话提交后的不透明状态标识，用于判断后续提交是否仍基于同一份会话历史。
_Avoid_: 消息数量、数据库行版本

**会话游标（Session Cursor）**:
会话有序回合中的不透明位置，用于继续读取更早的历史。
_Avoid_: 数组下标、数据库主键

**回合（Turn）**:
原子写入会话的一次用户输入和一次模型输出；只有已提交的回合才会交给记忆系统观察。
_Avoid_: 单条消息、模型调用

**回合标识（Turn ID）**:
由应用为一次逻辑回合提供的稳定标识；同一会话内重复使用时必须表示同一用户输入，已提交的模型输出是该回合的权威结果。
_Avoid_: Session ID、消息序号

**记忆系统（Memory System）**:
可插拔的记忆实现，在回合前返回记忆块，在回合提交后观察完整回合。
_Avoid_: 数据库、统一记忆仓库、Prompt Hook

**记忆绑定（Memory Binding）**:
应用或分身启用的一项记忆系统配置；绑定顺序也是记忆块的稳定输入顺序。
_Avoid_: 记忆系统实现、任意 Hook

**记忆块（Memory Block）**:
记忆系统返回的带来源标识的上下文内容，不隐含具体的模型角色或注入位置。
_Avoid_: System Prompt、统一记忆记录

**上下文（Model Context）**:
某一回合临时组装并发送给模型的输入，来源包括角色内容、会话消息、记忆块和当前输入。
_Avoid_: 会话、记忆仓库

**上下文组装（Context Assembly）**:
把回合的各类输入转换成模型上下文的策略，包括角色映射、注入位置、预算和裁剪顺序。
_Avoid_: Prompt Hook、记忆召回
