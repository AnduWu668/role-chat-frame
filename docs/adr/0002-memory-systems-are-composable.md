# 多个记忆系统通过最小契约组合

一个应用或分身可以绑定有序的多个记忆系统。框架在生成前调用每个系统的 `recall`，把返回结果保留为带来源的独立记忆块；在完成回合后调用每个系统的 `observe`。

公共契约只包含聊天编排必需的这两个能力。事件抽取、编译、向量检索、摘要、持久化、明文导出和管理操作都属于具体记忆系统，框架不强迫所有实现模拟同一套内部流程。

这允许静态世界书、用户档案、关系记忆、会话摘要和第三方记忆服务同时接入，也允许应用只启用其中一个。
