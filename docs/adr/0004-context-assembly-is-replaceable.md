# 上下文组装是一个可替换策略

框架把已解析的角色片段、分身覆盖片段、会话消息、有序记忆块和当前输入一次性交给上下文组装器。模型角色映射、注入位置、token 预算和裁剪顺序都由这个单一策略决定；记忆系统不能通过任意 Hook 直接修改 Prompt。

默认组装器按“角色定义、分身覆盖、会话消息、记忆块、当前输入”的顺序组装。超出已配置预算时只先移除最旧的会话消息，仍然超限则明确失败，不猜测哪些角色片段或记忆块可以丢弃。应用需要 CCv3 decorators、模型专用布局或其他预算策略时可以整体替换，因此后续能力可以接入而不扩大记忆系统的热路径接口。
