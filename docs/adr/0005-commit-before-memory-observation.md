# 回合提交后才通知记忆系统

用户输入和模型输出以一个完整回合按 `turnId` 原子提交，提交成功后才调用各记忆系统的 `observe`。提交前任一步失败都不产生完成回合；框架会尝试通知全部记忆系统，单个观察失败不阻止其他系统，也不回滚已经提交的回复，而是随成功结果单独报告。

重试发现同一 `turnId` 已经提交时，框架不再召回记忆、组装上下文或调用模型，而是恢复已保存的权威回合并再次交给各记忆系统观察。观察因此可能在恢复或重试时重复发生，记忆系统必须按 `turnId` 保证重复观察安全。
